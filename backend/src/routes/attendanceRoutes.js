const express = require('express');const router = express.Router();const {     getAttendanceStatus,     checkIn,     checkOut,     getMyAttendanceHistory,    getAllAttendance,    getMonthlySummary,    getAttendanceHistory,    getUserAttendance,    editAttendance} = require('../controllers/attendancecontroller');const { protect, authorize } = require('../middleware/authMiddleware');router.get('/', protect, authorize('view_hrms', 'view_attendance', 'hrms:attendance:view'), getAllAttendance);router.get('/all', protect, authorize('view_hrms', 'view_attendance', 'hrms:attendance:view'), getAllAttendance);
router.get('/history', protect, getAttendanceHistory);router.get('/monthly-summary', protect, authorize('view_hrms', 'view_attendance', 'hrms:attendance:view'), getMonthlySummary);router.get('/status', protect, getAttendanceStatus);router.post('/check-in', protect, checkIn);router.post('/checkin', protect, checkIn);
router.post('/check-out', protect, checkOut);router.post('/checkout', protect, checkOut);
router.get('/my-history', protect, getMyAttendanceHistory);router.get('/user/:id', protect, getUserAttendance);router.put('/edit', protect, authorize('manage_attendance', 'hrms:attendance:manage'), editAttendance);module.exports = router;