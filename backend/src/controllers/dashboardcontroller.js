const Material = require('../models/Material');const Employee = require('../models/Employee');const Order = require('../models/Order');const Customer = require('../models/Customer');const Vendor = require('../models/Vendor');const Salary = require('../models/Salary');const Attendance = require('../models/Attendance');const Leave = require('../models/Leave');const getDashboardStats = async (req, res) => {    try {        const role = req.user.role;        let stats = {};        try {            const materials = await Material.find({});            const activeMaterialsCount = materials.filter(m => m.isActive !== false).length;            const [totalEmployees, totalOrders, totalCustomers, activeCustomers, totalVendors] = await Promise.all([                Employee.countDocuments(),                Order.countDocuments(),                Customer.countDocuments(),                Customer.countDocuments({ status: 'Active' }),                Vendor.countDocuments()            ]);            stats = {                     totalMaterials: activeMaterialsCount,                     totalEmployees,                     totalOrders,                     totalCustomers,                     activeCustomers,                     totalVendors,                    trends: {                        employees: [],                        materials: [],                        customers: [],                        orders: [],                        revenue: [],                        attendance: [],                        payroll: []                    }                };        } catch (e) { console.error('Count Stats Error:', e); }        let revenue = 0;        let purchaseCost = 0;        try {            const revenueResult = await Order.aggregate([                { $match: { status: { $ne: 'Cancelled' }, orderType: 'sales', totalAmount: { $exists: true } } },                { $group: { _id: null, total: { $sum: "$totalAmount" } } }            ]);            revenue = (revenueResult && revenueResult.length > 0) ? revenueResult[0].total : 0;            if(stats.trends) stats.trends.revenue = [];            const purchaseResult = await Order.aggregate([                { $match: { status: { $ne: 'Cancelled' }, orderType: 'purchase', totalAmount: { $exists: true } } },                { $group: { _id: null, total: { $sum: "$totalAmount" } } }            ]);            purchaseCost = (purchaseResult && purchaseResult.length > 0) ? purchaseResult[0].total : 0;            const salesCount = await Order.countDocuments({ orderType: 'sales' });            const purchaseCount = await Order.countDocuments({ orderType: 'purchase' });            const activeOrdersCount = await Order.countDocuments({                status: { $nin: ["Completed", "Delivered", "Workflow Completed", "Invoice Generated", "Cancelled"] }            });            stats.totalSalesOrders = salesCount;            stats.totalPurchaseOrders = purchaseCount;            stats.activeOrdersCount = activeOrdersCount;        } catch (e) { console.error('Revenue Aggregation Error:', e); }        let lowStockMaterials = [];        let totalStockQuantity = 0;        let inTransitCount = 0;        let outOfStockCount = 0;        let allMaterialsRaw = [];        try {            allMaterialsRaw = await Material.find();            const allMaterials = allMaterialsRaw.filter(m => m.isActive !== false);            allMaterials.forEach(m => {                totalStockQuantity += (m.quantity || 0);                if (m.quantity <= (m.lowStockThreshold || 0)) {                    lowStockMaterials.push(m);                }                if ((m.quantity || 0) === 0) {                    outOfStockCount++;                }            });            const purchaseOrders = await Order.find({                 orderType: 'purchase',                 status: { $in: ['Pending', 'Awaiting Approval', 'Approved'] }             });            purchaseOrders.forEach(po => {                if (po.items && po.items.length > 0) {                    po.items.forEach(item => {                        inTransitCount += (item.quantity || 0);                    });                }            });        } catch (e) { console.error('Material Find Error:', e); }        let categoryData = [];        try {            const materialsList = await Material.find();            const activeMats = materialsList.filter(m => m.isActive !== false);            const catCounts = {};            activeMats.forEach(m => {                const cat = m.category || "Uncategorized";                catCounts[cat] = (catCounts[cat] || 0) + 1;            });            categoryData = Object.keys(catCounts).map(cat => ({ name: cat, value: catCounts[cat] }));        } catch (e) { console.error('Category Aggregation Error:', e); }        let monthlyStats = [];        try {            const monthlyStatsRaw = await Order.aggregate([                { $match: { createdAt: { $exists: true } } },                {                    $group: {                        _id: { $month: "$createdAt" },                        sales: { $sum: 1 },                        revenue: { $sum: "$totalAmount" }                    }                },                { $sort: { "_id": 1 } },                {                    $project: {                        name: {                            $arrayElemAt: [                                ["", "Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"],                                { $ifNull: ["$_id", 0] }                            ]                        },                        sales: 1,                        revenue: 1                    }                }            ]);            const allMonths = ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"];            monthlyStats = allMonths.map((monthName, index) => {                const found = monthlyStatsRaw?.find(m => m.name === monthName);                return found || { name: monthName, sales: 0, revenue: 0 };            });            if (monthlyStats.every(m => m.revenue === 0 && m.sales === 0)) {                monthlyStats = [];            }        } catch (e) { console.error('Monthly Stats Aggregation Error:', e); }        let topSellingMaterials = [];        let salesCategoryData = [];        try {            const salesOrders = await Order.find({ orderType: 'sales', status: { $ne: 'Cancelled' } });            const matNameMap = {};            const matCatMap = {};            allMaterialsRaw.forEach(m => {                matNameMap[m._id.toString()] = m.name;                matCatMap[m._id.toString()] = m.category || 'General';            });            let materialSalesMap = {};            let salesCatMap = {};            salesOrders.forEach(order => {                if (order.items && Array.isArray(order.items)) {                    order.items.forEach(item => {                        const matId = String(item.material);                        if (matId && matId !== 'undefined') {                            if (!materialSalesMap[matId]) {                                materialSalesMap[matId] = { quantity: 0, revenue: 0 };                            }                            materialSalesMap[matId].quantity += (item.quantity || 0);                            const rev = ((item.quantity || 0) * (item.price || item.unitPrice || 0));                            materialSalesMap[matId].revenue += rev;                            const cat = matCatMap[matId] || 'General';                            salesCatMap[cat] = (salesCatMap[cat] || 0) + rev;                        }                    });                }            });            topSellingMaterials = Object.keys(materialSalesMap).map(matId => ({                id: matId,                name: matNameMap[matId] || 'Unknown Material',                category: matCatMap[matId] || 'General',                sales: materialSalesMap[matId].quantity,                revenue: materialSalesMap[matId].revenue            })).sort((a, b) => b.sales - a.sales).slice(0, 5);            salesCategoryData = Object.keys(salesCatMap).map(cat => ({                name: cat,                value: salesCatMap[cat]            }));        } catch (e) { console.error('Top Selling Calculation Error:', e); }        let recentOrders = [];        try {            recentOrders = await Order.find()                .sort({ createdAt: -1 })                .limit(5);        } catch (e) { console.error('Recent Orders Find Error:', e); }        let pendingSalaries = [];        try {            pendingSalaries = await Salary.find({ status: 'Awaiting Approval' })                .sort({ createdAt: -1 });        } catch (e) { console.error('Pending Salaries Find Error:', e); }        let recentActivity = [];        let notifications = [];        try {            const Notification = require('../models/Notification');            const AuditLog = require('../models/AuditLog');            let notifQuery = {};
            if (role !== 'Admin') {
                notifQuery = {
                    $or: [
                        { userId: null, role: null },
                        { userId: req.user._id },
                        { userId: req.user.id },
                        { role: req.user.role }
                    ]
                };
            }
            const notifs = await Notification.find(notifQuery).sort({ createdAt: -1 }).limit(5);            notifications = notifs.map(n => ({                id: n._id,                text: n.message || n.title,                category: n.category || 'general',                type: n.type || 'info',                time: n.createdAt            }));            const rNameActivity = (role || '').toLowerCase();            if (rNameActivity === 'employee') {                // ── Employee-specific activity feed ──                try {                    const Task = require('../models/Task');                    const empRec = await Employee.findOne({ userId: req.user.id });                    const empActivities = [];                    if (empRec) {                        const empId = empRec._id || empRec.id;                        // Attendance events (last 10)                        const attRecs = await Attendance.find({ employeeId: empId })                            .sort({ date: -1 }).limit(10);                        attRecs.forEach(a => {                            if (a.checkIn) {                                empActivities.push({                                    id: `att-in-${a._id || a.id}`,                                    iconType: 'checkin',                                    title: 'Checked In',                                    text: `Attendance marked — Status: ${a.status || 'Present'}`,                                    type: 'attendance',                                    time: a.checkIn || a.date || a.createdAt                                });                            }                            if (a.checkOut) {                                empActivities.push({                                    id: `att-out-${a._id || a.id}`,                                    iconType: 'checkout',                                    title: 'Checked Out',                                    text: `Work session ended — ${a.totalHours ? a.totalHours.toFixed(1) + ' hrs logged' : 'no hours recorded'}`,                                    type: 'attendance',                                    time: a.checkOut                                });                            }                        });                        // Task events (last 5)                        const allTasks = await Task.find({});                        const myTasks = allTasks.filter(t => {                            let assigned = t.assignedTo;                            if (typeof assigned === 'string') try { assigned = JSON.parse(assigned); } catch(e2) { assigned = []; }                            if (!Array.isArray(assigned)) assigned = [];                            return assigned.some(id => String(id) === String(empId));                        });                        const recentTasks = myTasks.sort((a, b) => new Date(b.updatedAt || b.createdAt) - new Date(a.updatedAt || a.createdAt)).slice(0, 5);                        recentTasks.forEach(t => {                            let comps = t.completions;                            if (typeof comps === 'string') try { comps = JSON.parse(comps); } catch(e2) { comps = []; }                            if (!Array.isArray(comps)) comps = [];                            const myComp = comps.find(c => String(c.user?.id || c.user?._id || c.user) === String(empId));                            const isCompleted = myComp && (myComp.status === 'Completed' || myComp.status === 'Done');                            empActivities.push({                                id: `task-${t._id || t.id}`,                                iconType: isCompleted ? 'task_done' : 'task_assigned',                                title: isCompleted ? 'Task Completed' : 'Task Assigned',                                text: `"${t.title}" · Priority: ${t.priority || 'Medium'}`,                                type: 'task',                                time: t.updatedAt || t.createdAt                            });                        });                        // Leave events (last 5)                        const leaveRecs = await Leave.find({ employeeId: empId })                            .sort({ createdAt: -1 }).limit(5);                        leaveRecs.forEach(l => {                            const statusLabel = l.status === 'Approved' ? 'Approved' : l.status === 'Rejected' ? 'Rejected' : 'Submitted';                            empActivities.push({                                id: `leave-${l._id || l.id}`,                                iconType: 'leave',                                title: `Leave ${statusLabel}`,                                text: `${l.type} Leave — ${new Date(l.startDate).toLocaleDateString('en-IN', {day:'2-digit',month:'short'})} to ${new Date(l.endDate).toLocaleDateString('en-IN', {day:'2-digit',month:'short'})}`,                                type: 'leave',                                time: l.updatedAt || l.createdAt                            });                        });                        // Payslip events (last 3)                        const salaryRecs = await Salary.find({ employeeId: empId })                            .sort({ createdAt: -1 }).limit(3);                        salaryRecs.forEach(s => {                            empActivities.push({                                id: `salary-${s._id || s.id}`,                                iconType: 'payslip',                                title: 'Payslip Generated',                                text: `Salary for ${s.month || 'this month'} — ₹${(s.netSalary || 0).toLocaleString('en-IN')}`,                                type: 'salary',                                time: s.createdAt                            });                        });                    }                    empActivities.sort((a, b) => new Date(b.time) - new Date(a.time));                    recentActivity = empActivities.slice(0, 10);                } catch (empActErr) {                    console.error('Employee Activity Feed Error:', empActErr.message);                    recentActivity = [];                }            } else if (rNameActivity === 'sales') {                // ── Sales-specific activity feed ──                try {                    const Lead = require('../models/Lead');                    const Quotation = require('../models/Quotation');                    const salesActivities = [];                    const userId = req.user.id;                    // Lead events                    const leadRecs = await Lead.find().sort({ updatedAt: -1 }).limit(8).catch(() => []);                    leadRecs.forEach(l => {                        const isNew = new Date(l.createdAt) > new Date(Date.now() - 48 * 3600 * 1000);                        salesActivities.push({                            id: `lead-${l._id || l.id}`,                            iconType: isNew ? 'lead_new' : 'lead_updated',                            title: isNew ? 'New Lead Assigned' : 'Lead Status Updated',                            text: `${l.name}${l.companyName ? ' · ' + l.companyName : ''} — ${l.status}`,                            type: 'lead',                            time: l.updatedAt || l.createdAt                        });                    });                    // Sales Order events                    const salesOrders = await Order.find({ orderType: 'sales' }).sort({ updatedAt: -1 }).limit(5).catch(() => []);                    salesOrders.forEach(o => {                        const isDispatched = ['Dispatched', 'Delivered', 'Workflow Completed'].includes(o.status);                        const isWon = o.status === 'Delivered' || o.status === 'Workflow Completed';                        salesActivities.push({                            id: `order-${o._id || o.id}`,                            iconType: isDispatched ? (isWon ? 'deal_won' : 'order_dispatched') : 'order_created',                            title: isDispatched ? (isWon ? 'Deal Won' : 'Order Dispatched') : 'Sales Order Created',                            text: `Order #${o.orderNumber || o.id} · ₹${(o.totalAmount || o.grandTotal || 0).toLocaleString('en-IN')} — ${o.status}`,                            type: 'order',                            time: o.updatedAt || o.createdAt                        });                    });                    // Quotation events                    const quoteRecs = await Quotation.find().sort({ createdAt: -1 }).limit(5).catch(() => []);                    quoteRecs.forEach(q => {                        salesActivities.push({                            id: `quote-${q._id || q.id}`,                            iconType: 'quotation',                            title: 'Quotation Created',                            text: `${q.quotationNumber} for ${q.customerName} · ₹${(q.grandTotal || 0).toLocaleString('en-IN')}`,                            type: 'quotation',                            time: q.createdAt                        });                    });                    salesActivities.sort((a, b) => new Date(b.time) - new Date(a.time));                    recentActivity = salesActivities.slice(0, 10);                } catch (salesActErr) {                    console.error('Sales Activity Feed Error:', salesActErr.message);                    recentActivity = [];                }            } else {                // ── Default: Admin / Manager / HR — AuditLog ──                const audits = await AuditLog.find().sort({ createdAt: -1 }).limit(10).catch(() => []);                recentActivity = audits.map(a => ({                    id: a._id,                    iconType: 'system',                    title: a.action === 'CREATE' ? 'Record Created' : a.action === 'DELETE' ? 'Record Deleted' : 'Record Updated',                    text: a.description || `${a.action} performed on ${a.module}`,                    category: 'system',                    type: a.action === 'CREATE' ? 'success' : (a.action === 'DELETE' ? 'warning' : 'info'),                    time: a.createdAt || a.updatedAt                }));            }        } catch (e) { console.error('Activity/Notification Error:', e); }        let data = {                hrStats: {                    totalEmployees: stats.totalEmployees || 0,                    attendanceRate: '0%',                    onLeave: 0,                    newJoiners: 0,                    attendanceHistory: [],                    employeeDistribution: []                },            totalEmployees: stats.totalEmployees || 0,            totalMaterials: stats.totalMaterials || 0,            activeCustomers: stats.activeCustomers || 0,            totalCustomers: stats.totalCustomers || 0,            openOrders: (await Order.countDocuments({ status: { $nin: ['Delivered', 'Completed', 'Workflow Completed', 'Invoice Generated', 'Cancelled'] } })) || 0,            lowStockItems: lowStockMaterials.length,            totalStockQuantity: totalStockQuantity,            totalRevenue: revenue,            stats: {                 ...stats,                revenue,                purchaseCost,                pendingOrders: await Order.countDocuments({ status: 'Awaiting Approval' }),                pendingSalaries: await Salary.countDocuments({ status: 'Awaiting Approval' }),                pendingCustomers: await Customer.countDocuments({ status: 'Pending Review' })            },            materialStats: {                totalMaterialTypes: stats.totalMaterials || 0,                totalStockQuantity: totalStockQuantity,                lowStockCount: lowStockMaterials.length,                inTransitCount: inTransitCount,                outOfStockCount: outOfStockCount            },            charts: {                 monthlyStats,                 categoryData: categoryData || [],                salesCategoryData: salesCategoryData || [],                hrmsDonut: [                    { name: 'Active', value: stats.totalEmployees || 0, color: '#3b82f6' },                    { name: 'Pending', value: 0, color: '#f59e0b' }                ],                matDonut: [                    { name: 'In Stock', value: (stats.totalMaterials || 0) - lowStockMaterials.length, color: '#10b981' },                    { name: 'Low Stock', value: lowStockMaterials.length, color: '#f59e0b' },                    { name: 'In Transit', value: inTransitCount, color: '#3b82f6' }                ],                crmDonut: [                    { name: 'Active', value: stats.activeCustomers || 0, color: '#10b981' },                    { name: 'Pending', value: await Customer.countDocuments({status: 'Pending Review'}), color: '#f59e0b' },                    { name: 'Inactive', value: (stats.totalCustomers || 0) - (stats.activeCustomers || 0), color: '#ef4444' }                ],                erpDonut: [                    { name: 'Sales', value: stats.totalSalesOrders || 0, color: '#3b82f6' },                    { name: 'Purchase', value: stats.totalPurchaseOrders || 0, color: '#10b981' },                    { name: 'Pending', value: await Order.countDocuments({status: 'Awaiting Approval'}), color: '#f59e0b' }                ]            },            tables: {                 lowStock: lowStockMaterials,                 recentOrders: recentOrders || [],                pendingSalaries: pendingSalaries || [],                recentActivity: recentActivity || [],                notifications: notifications || [],                topSellingMaterials: topSellingMaterials || []            }        };        try {            const allVendors = await Vendor.find({});            const vendorsByCategory = {};            allVendors.forEach(v => {                const cat = v.category || 'Other';                vendorsByCategory[cat] = (vendorsByCategory[cat] || 0) + 1;            });            data.vendorStats = {                totalVendors: stats.totalVendors || allVendors.length,                vendorsByCategory: Object.entries(vendorsByCategory).map(([name, value]) => ({ name, value }))            };        } catch (e) { console.error('Vendor Stats Error:', e); }        try {            const payrollRaw = await Salary.aggregate([                { $match: { status: 'Approved', month: { $exists: true } } },                {                    $group: {                        _id: "$month",                        amount: { $sum: "$netSalary" }                    }                },                { $sort: { "_id": 1 } },                { $limit: 6 }            ]);            data.charts.payrollData = payrollRaw.map(p => ({                name: p._id,
                amount: p.amount            }));            if (data.charts.payrollData.length === 0) {                 data.charts.payrollData = [];            }        } catch (e) { console.error('Payroll Aggregation Error:', e); }        if (role === 'HR' || role === 'Admin') {            try {                const todayStart = new Date();                todayStart.setHours(0,0,0,0);                const todayEnd = new Date();                todayEnd.setHours(23,59,59,999);                const activeEmployeesCount = await Employee.countDocuments({});                const thirtyDaysAgo = new Date();                thirtyDaysAgo.setDate(thirtyDaysAgo.getDate() - 30);                const newJoinersCount = await Employee.countDocuments({                    joinDate: { $gte: thirtyDaysAgo }                });                const deptStats = await Employee.aggregate([                    { $group: { _id: "$department", value: { $sum: 1 } } }                ]);                const COLORS = ['#2563eb', '#10b981', '#f59e0b', '#7c3aed', '#0d9488'];                const employeeDistribution = deptStats.map((d, index) => ({                    name: d._id || 'Other',                    value: d.value,                    percentage: `${((d.value / activeEmployeesCount) * 100).toFixed(1)}%`,                    color: COLORS[index % COLORS.length]                }));                const recentEmployees = await Employee.find()                    .sort({ createdAt: -1 })                    .limit(4);                const recentEmployeesFormatted = recentEmployees.map(emp => ({                    name: `${emp.firstName} ${emp.lastName || ''}`.trim(),                    role: emp.designation || 'Staff',                    avatar: `${emp.firstName[0] || ''}${emp.lastName ? emp.lastName[0] : ''}`.toUpperCase()                }));                const attendanceHistory = [];                for (let i = 4; i >= 0; i--) {                    const d = new Date();                    d.setDate(d.getDate() - i);                    d.setHours(0,0,0,0);                    const dEnd = new Date(d);                    dEnd.setHours(23,59,59,999);                    const count = await Attendance.countDocuments({                        date: { $gte: d, $lte: dEnd },                        status: { $in: ['Present', 'Late'] }                    });                    const dayName = d.toLocaleDateString('en-US', { weekday: 'short' });                    attendanceHistory.push({ name: dayName, employees: count });                }                const allAttendances = await Attendance.find({ date: { $gte: todayStart, $lte: todayEnd } });                const allLeaves = await Leave.find({ status: 'Approved', startDate: { $lte: todayEnd }, endDate: { $gte: todayStart } });                const attendanceMap = {};                allAttendances.forEach(a => { attendanceMap[a.employeeId?.toString()] = a; });                const leaveMap = {};                allLeaves.forEach(l => { leaveMap[l.employeeId?.toString()] = true; });                const now = new Date();                const defaultStatus = '-';                const allEmployees = await Employee.find({});                let presentToday = 0;                let absentToday = 0;                allEmployees.forEach(emp => {                    const empId = emp.id?.toString();                    let finalStatus = defaultStatus;                    if (attendanceMap[empId]) {                        finalStatus = attendanceMap[empId].status;                    } else if (leaveMap[empId]) {                        finalStatus = 'On Leave';                    } else {                        finalStatus = 'Absent';                    }                    if (finalStatus === 'Present' || finalStatus === 'Late') presentToday++;                    if (finalStatus === 'Absent') absentToday++;                });                const onLeave = await Leave.countDocuments({ status: 'Approved', startDate: { $lte: todayEnd }, endDate: { $gte: todayStart } });                const pendingLeaves = await Leave.countDocuments({ status: 'Pending' });                let salaryBrackets = { '< ₹30k': 0, '₹30k - 50k': 0, '₹50k - 80k': 0, '₹80k - 120k': 0, '> ₹120k': 0 };                allEmployees.forEach(emp => {                    const s = emp.salary || 0;                    if (s < 30000) salaryBrackets['< ₹30k']++;                    else if (s <= 50000) salaryBrackets['₹30k - 50k']++;                    else if (s <= 80000) salaryBrackets['₹50k - 80k']++;                    else if (s <= 120000) salaryBrackets['₹80k - 120k']++;                    else salaryBrackets['> ₹120k']++;                });                const salaryDistribution = Object.keys(salaryBrackets).map(key => ({ range: key, count: salaryBrackets[key] }));                data.hrStats = {                    totalEmployees: activeEmployeesCount,                    presentToday: presentToday,                    onLeave: onLeave,                    pending: pendingLeaves,                    absentToday: absentToday,                    newJoiners: newJoinersCount,                    employeeDistribution,                    salaryDistribution,                    recentEmployees: recentEmployeesFormatted,                    attendanceHistory                };                if (data.charts && data.charts.hrmsDonut) {                    data.charts.hrmsDonut = [                        { name: 'Present', value: presentToday, color: '#10b981' },                        { name: 'Absent', value: absentToday, color: '#ef4444' },                        { name: 'On Leave', value: onLeave, color: '#f59e0b' }                    ];                }            } catch (err) {                console.error('HR Dashboard Stats Error:', err);                data.hrStats = { totalEmployees: stats.totalEmployees, presentToday: 0, onLeave: 0, newJoiners: 0, employeeDistribution: [], recentEmployees: [], attendanceHistory: [] };            }        }                 if (role === 'Sales' || role === 'Admin') {
            try {
                const Lead = require('../models/Lead');
                const Quotation = require('../models/Quotation');
                const firstDayOfMonth = new Date(new Date().getFullYear(), new Date().getMonth(), 1);
                data.salesStats = {
                    recentCustomers: await Customer.countDocuments({ createdAt: { $gte: firstDayOfMonth } })
                };
                // Build sales pipeline trend: 6-month rolling window
                const mNames2 = ['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'];
                const currentYear3 = new Date().getFullYear();
                const currentMonth3 = new Date().getMonth();
                const allLeads = await Lead.find({}).catch(() => []);
                const allOrders3 = await Order.find({ orderType: 'sales' }).catch(() => []);
                const allQuotes = await Quotation.find({}).catch(() => []);
                const salesTrend = [];
                for (let i = 5; i >= 0; i--) {
                    let mIdx = currentMonth3 - i;
                    let yr = currentYear3;
                    if (mIdx < 0) { mIdx += 12; yr -= 1; }
                    const mStart = new Date(yr, mIdx, 1);
                    const mEnd = new Date(yr, mIdx + 1, 0, 23, 59, 59, 999);
                    const inMonth = (dateVal) => {
                        const d = new Date(dateVal);
                        return !isNaN(d) && d >= mStart && d <= mEnd;
                    };
                    const newLeadsCount = allLeads.filter(l => inMonth(l.createdAt)).length;
                    const dealsClosedCount = allLeads.filter(l =>
                        (l.status === 'Converted' || l.status === 'Won') && inMonth(l.updatedAt || l.createdAt)
                    ).length + allOrders3.filter(o =>
                        ['Delivered','Workflow Completed'].includes(o.status) && inMonth(o.updatedAt || o.createdAt)
                    ).length;
                    const quotesCount = allQuotes.filter(q => inMonth(q.createdAt)).length;
                    salesTrend.push({
                        name: mNames2[mIdx],
                        newLeads: newLeadsCount,
                        dealsClosed: dealsClosedCount,
                        meetings: quotesCount   // Use quotations as a proxy for "meetings/proposals"
                    });
                }
                // Also build last-year comparison
                const salesTrendLast = [];
                for (let i = 5; i >= 0; i--) {
                    let mIdx = currentMonth3 - i;
                    let yr = currentYear3 - 1;
                    if (mIdx < 0) { mIdx += 12; yr -= 1; }
                    const mStart = new Date(yr, mIdx, 1);
                    const mEnd = new Date(yr, mIdx + 1, 0, 23, 59, 59, 999);
                    const inMonth = (dateVal) => {
                        const d = new Date(dateVal);
                        return !isNaN(d) && d >= mStart && d <= mEnd;
                    };
                    salesTrendLast.push({
                        name: mNames2[mIdx],
                        lastNewLeads: allLeads.filter(l => inMonth(l.createdAt)).length,
                        lastDealsClosed: allLeads.filter(l =>
                            (l.status === 'Converted' || l.status === 'Won') && inMonth(l.updatedAt || l.createdAt)
                        ).length,
                        lastMeetings: allQuotes.filter(q => inMonth(q.createdAt)).length
                    });
                }
                // Merge current + last year into single array
                const mergedSalesTrend = salesTrend.map((item, idx) => ({
                    ...item,
                    ...(salesTrendLast[idx] || {})
                }));
                data.analytics = data.analytics || {};
                data.analytics.salesTrend = mergedSalesTrend;
            } catch (e) { console.error('Sales Pipeline Aggregation Error:', e.message); }
        }        if (role === 'Manager' || role === 'Admin') {            try {                data.managerStats = {                    teamMembers: stats.totalEmployees,                    activeProjects: await Order.countDocuments({ status: { $in: ['Pending', 'Awaiting Approval', 'Approved', 'In Progress'] } }),                    pendingApprovals: (await Order.countDocuments({ status: 'Awaiting Approval' })) + (await Leave.countDocuments({ status: 'Pending' })),                    teamProductivity: 0
                };            } catch (e) { console.error('Manager Stats Error:', e); }        }        const roleName = (role || '').toLowerCase();        if (roleName === 'employee') {             try {                 const empRecord = await Employee.findOne({ userId: req.user.id });                 if (empRecord) {                     const todayStart = new Date(); todayStart.setHours(0,0,0,0);                     const todayEnd = new Date(); todayEnd.setHours(23,59,59,999);                     const att = await Attendance.findOne({ employeeId: empRecord._id, date: { $gte: todayStart, $lte: todayEnd } });                     // Compute real training completion rate                     let trainingCompletion = null;                     let kudos = null;                     let performanceScore = null;                     try {                         const { TrainingEnrollment } = require('../models/Training');                         const empUserId = empRecord.userId || req.user.id;                         const enrollments = await TrainingEnrollment.find({ userId: empUserId });                         if (enrollments.length > 0) {                             const completed = enrollments.filter(e => e.status === 'Completed').length;                             trainingCompletion = Math.round((completed / enrollments.length) * 100);                         } else {                             trainingCompletion = 0;                         }                     } catch(te) { console.error('Training completion error:', te.message); }                     data.employeeStats = {                         attendanceToday: att ? att.status : '-',                         myPendingLeaves: await Leave.countDocuments({ employeeId: empRecord._id, status: 'Pending' }),                         trainingCompletion,                         kudos,                         performanceScore                     };                 }             } catch(e) { console.error('Employee Stats Error:', e); }        }        try {            const completedSalesOrders = await Order.find({ orderType: 'sales', status: { $ne: 'Cancelled' } });            const completedPurchaseOrders = await Order.find({ orderType: 'purchase', status: { $ne: 'Cancelled' } });            const totalAnalyticsRevenue = completedSalesOrders.reduce((sum, o) => sum + (Number(o.totalAmount) || Number(o.grandTotal) || 0), 0);            const totalAnalyticsExpenses = completedPurchaseOrders.reduce((sum, o) => sum + (Number(o.totalAmount) || Number(o.grandTotal) || 0), 0);            const netProfit = totalAnalyticsRevenue - totalAnalyticsExpenses;            const currentMonth = new Date().getMonth();            const thisMonthRev = completedSalesOrders.filter(o => { const d = new Date(o.orderDate || o.createdAt); return !isNaN(d) && d.getMonth() === currentMonth; }).reduce((s, o) => s + (Number(o.totalAmount) || Number(o.grandTotal) || 0), 0);            const lastMonthRev = completedSalesOrders.filter(o => { const d = new Date(o.orderDate || o.createdAt); return !isNaN(d) && d.getMonth() === ((currentMonth - 1 + 12) % 12); }).reduce((s, o) => s + (Number(o.totalAmount) || Number(o.grandTotal) || 0), 0);            const revenueGrowth = lastMonthRev > 0 ? ((thisMonthRev - lastMonthRev) / lastMonthRev * 100).toFixed(1) : (thisMonthRev > 0 ? 100 : 0);            const trendData = [];            const monthNames = ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"];            const currentYear = new Date().getFullYear();            const hasHistorical = completedSalesOrders.some(o => {                const d = new Date(o.orderDate || o.createdAt);                return !isNaN(d) && d.getMonth() !== currentMonth;            });            let currentYearTotalProfit = 0;            let lastYearTotalProfit = 0;            for (let i = 0; i < 12; i++) {                let cyRev = completedSalesOrders.filter(o => { const d = new Date(o.orderDate || o.createdAt); return !isNaN(d) && d.getMonth() === i && d.getFullYear() === currentYear; }).reduce((s, o) => s + (Number(o.totalAmount) || Number(o.grandTotal) || 0), 0);                let cyExp = completedPurchaseOrders.filter(o => { const d = new Date(o.orderDate || o.createdAt); return !isNaN(d) && d.getMonth() === i && d.getFullYear() === currentYear; }).reduce((s, o) => s + (Number(o.totalAmount) || Number(o.grandTotal) || 0), 0);                let lyRev = completedSalesOrders.filter(o => { const d = new Date(o.orderDate || o.createdAt); return !isNaN(d) && d.getMonth() === i && d.getFullYear() === currentYear - 1; }).reduce((s, o) => s + (Number(o.totalAmount) || Number(o.grandTotal) || 0), 0);                let lyExp = completedPurchaseOrders.filter(o => { const d = new Date(o.orderDate || o.createdAt); return !isNaN(d) && d.getMonth() === i && d.getFullYear() === currentYear - 1; }).reduce((s, o) => s + (Number(o.totalAmount) || Number(o.grandTotal) || 0), 0);                const cyProfit = cyRev - cyExp;                const lyProfit = lyRev - lyExp;                if (i <= currentMonth || hasHistorical) currentYearTotalProfit += cyProfit;                lastYearTotalProfit += lyProfit;                trendData.push({                    name: monthNames[i],                    fullMonth: ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"][i],                    currentYearProfit: Math.round(cyProfit),                    lastYearProfit: Math.round(lyProfit),                    revenue: Math.round(cyRev),                    expenses: Math.round(cyExp),                    lastYearRevenue: Math.round(lyRev),                    lastYearExpenses: Math.round(lyExp)                });            }            const totalMatCount = stats.totalMaterials || 0;            const materialHealth = totalMatCount > 0 ? Math.round(((totalMatCount - lowStockMaterials.length) / totalMatCount) * 100) : 0;            let hrAttendanceRate = 0;            if (data.hrStats && data.hrStats.totalEmployees > 0) {                hrAttendanceRate = Math.round(((data.hrStats.presentToday || 0) / data.hrStats.totalEmployees) * 100);            }            const totalOrderCount = await Order.countDocuments();            const fulfilledOrderCount = await Order.countDocuments({ status: { $in: ['Delivered', 'Completed', 'Workflow Completed', 'Invoice Generated'] } });            const orderFulfillment = totalOrderCount > 0 ? Math.round((fulfilledOrderCount / totalOrderCount) * 100) : 0;            const totalCustCount = stats.totalCustomers || 0;            const customerRetention = totalCustCount > 0 ? Math.round(((stats.activeCustomers || 0) / totalCustCount) * 100) : 0;            data.analytics = {                kpis: {                    totalRevenue: totalAnalyticsRevenue,                    totalExpenses: totalAnalyticsExpenses,                    netProfit: netProfit,                    revenueGrowth: Number(revenueGrowth),                    currentYearTotalProfit,                    lastYearTotalProfit,                    thisMonthRevenue: thisMonthRev,                    lastMonthRevenue: lastMonthRev                },                trendData: trendData.every(t => t.revenue === 0 && t.lastYearRevenue === 0) ? [] : trendData,                healthMetrics: {                    materialHealth,                    hrAttendanceRate,                    orderFulfillment,                    customerRetention                }            };        } catch (e) {             console.error('Analytics Data Error:', e.message);            data.analytics = { trendData: [], kpis: { totalRevenue: revenue, totalExpenses: purchaseCost, netProfit: revenue - purchaseCost, revenueGrowth: 0 }, healthMetrics: { materialHealth: 0, hrAttendanceRate: 0, orderFulfillment: 0, customerRetention: 0 } };        }        data.systemInfo = {            currentFY: "2026 - 2027",            erpVersion: "v2.5.1",            dbSize: "1.28 GB",            lastBackup: ""        };        try {            const now = new Date();            data.systemInfo.currentFY = now.getMonth() >= 3                 ? `${now.getFullYear()} - ${now.getFullYear() + 1}`                 : `${now.getFullYear() - 1} - ${now.getFullYear()}`;            const lastBackup = new Date();            lastBackup.setHours(lastBackup.getHours() - 2);
            const monthNames = ["Jan", "Feb", "Mar", "Apr", "May", "Jun", "Jul", "Aug", "Sep", "Oct", "Nov", "Dec"];            data.systemInfo.lastBackup = `${String(lastBackup.getDate()).padStart(2, '0')} ${monthNames[lastBackup.getMonth()]} ${lastBackup.getFullYear()}, ${lastBackup.toLocaleTimeString([], {hour: '2-digit', minute:'2-digit'})}`;            try {                const pkg = require('../../package.json');                if (pkg && pkg.version) data.systemInfo.erpVersion = "v" + pkg.version;            } catch (err) {}        try {            if (Material.db && Material.db.db) {                const stats = await Material.db.db.command({ dbStats: 1 });                if (stats && stats.dataSize) {                    data.systemInfo.dbSize = (stats.dataSize / (1024 * 1024)).toFixed(2) + " MB";                }            }        } catch (err) { console.error('DB Stats Error:', err.message); }        } catch (e) { console.error('System Info Error:', e); }        const rName = (role || '').toLowerCase();        if (rName === 'employee') {             data.analytics = data.analytics || {};             try {                 const Task = require('../models/Task');                 const empRecord2 = await Employee.findOne({ userId: req.user.id });                 const mNames = ['Jan','Feb','Mar','Apr','May','Jun','Jul','Aug','Sep','Oct','Nov','Dec'];                 const currentYear2 = new Date().getFullYear();                 const employeeTrend = [];                 for (let i = 0; i < 12; i++) {                     const monthStart = new Date(currentYear2, i, 1);                     const monthEnd = new Date(currentYear2, i + 1, 0, 23, 59, 59, 999);                     const lastYearStart = new Date(currentYear2 - 1, i, 1);                     const lastYearEnd = new Date(currentYear2 - 1, i + 1, 0, 23, 59, 59, 999);                     let tasksCompleted = 0, lastTasksCompleted = 0, hoursLogged = 0, lastHoursLogged = 0;                     if (empRecord2) {                         const empId = empRecord2._id || empRecord2.id;                         const allTasks = await Task.find({ assignedTo: empId });                         allTasks.forEach(t => {                             const completions = Array.isArray(t.completions) ? t.completions : [];                             completions.forEach(c => {                                 const cd = new Date(c.completedAt || c.date);                                 if (!isNaN(cd)) {                                     if (cd >= monthStart && cd <= monthEnd) tasksCompleted++;                                     if (cd >= lastYearStart && cd <= lastYearEnd) lastTasksCompleted++;                                 }                             });                             if (!t.completions || t.completions.length === 0) {                                 const td = new Date(t.updatedAt || t.createdAt);                                 if (t.status === 'Done' || t.status === 'Completed') {                                     if (!isNaN(td)) {                                         if (td >= monthStart && td <= monthEnd) tasksCompleted++;                                         if (td >= lastYearStart && td <= lastYearEnd) lastTasksCompleted++;                                     }                                 }                             }                         });                         const attRecords = await Attendance.find({ employeeId: empId, date: { $gte: monthStart, $lte: monthEnd } });                         hoursLogged = attRecords.reduce((sum, a) => sum + (a.totalHours || 0), 0);                         const lastAttRecords = await Attendance.find({ employeeId: empId, date: { $gte: lastYearStart, $lte: lastYearEnd } });                         lastHoursLogged = lastAttRecords.reduce((sum, a) => sum + (a.totalHours || 0), 0);                     }                     const efficiency = hoursLogged > 0 && tasksCompleted > 0 ? Math.min(100, Math.round((tasksCompleted / (hoursLogged / 8)) * 100)) : 0;                     const lastEfficiency = lastHoursLogged > 0 && lastTasksCompleted > 0 ? Math.min(100, Math.round((lastTasksCompleted / (lastHoursLogged / 8)) * 100)) : 0;                     employeeTrend.push({ name: mNames[i], tasksCompleted, lastTasksCompleted, hoursLogged: Math.round(hoursLogged), lastHoursLogged: Math.round(lastHoursLogged), efficiency, lastEfficiency });                 }                 data.analytics.employeeTrend = employeeTrend;             } catch (e) {                 console.error('Employee Trend Error:', e.message);                 data.analytics.employeeTrend = [];             }        }        res.json(data);    } catch (error) {        console.error('Final Dashboard Stats Error:', error);        res.status(500).json({ message: error.message });    }};module.exports = { getDashboardStats };